package model.cards;

import model.enums.EventType;

public class EventCard extends TribeCard {
    private final EventType eventType; // HUNT, SHAMANIC_RITUAL, CAVE_PAINTINGS, SUSTENANCE
                                       // + FINAL_EVENT for the last two events of the game

    public EventType getEventType()
}