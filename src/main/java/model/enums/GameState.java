package model.enums;

public enum GameState {
    SETUP,    // the game has not yet started
    RUNNING,  // the game is in progress
    ENDED     // the game has ended and the final scores have been calculated
}
